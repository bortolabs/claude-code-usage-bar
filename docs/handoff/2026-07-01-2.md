# Handoff — 01/07/2026 (2)

**Chat:** `CCUB-S2_2026-07-01_hierarquia-anomalias-copiloto`
**Branch:** `master`
**Último commit:** `7dc7acb — feat: hierarquia de 3 tiers p/ anomalias/dicas/burn rate (v0.37.0)`
**Release:** `v0.37.0` (CI → Open VSX + GitHub Release)

## Feito
- **Hierarquia de 3 tiers (v0.37.0)** p/ anomalias · dicas · burn rate. Tier 1 (alerta): anomalia crítica ⛔ sobe pro banner global. Tier 2 (orientação): anomalias ⚠ warn entram no card **Copiloto** na aba Sessão. Tier 3 (análise): dicas seguem só na Custos.
- **Escalonamento anomalia→dica:** `suppressCoveredTips` remove a dica coberta (`mcpRunaway`→mcp, `ctxInflated`→context) quando a anomalia dispara — fim da duplicação. `cacheLow` fica de fora (oposta à dica de cache-read).
- **Burn rate domina cota:** com o alerta ativo, o advice `fitsUntilReset` ("Cabem ~X") é suprimido (fim do "vai estourar" × "cabem 39M" lado a lado).
- Validado no F5 (banner cross-aba, Copiloto com warns, dicas cobertas sumindo). +5 testes → 97 no total, typecheck verde.

## Decisões
- Anomalias na Sessão, não só Custos: o card crítico só era visto via notificação opt-in (default off) → furo de discoverability. Copiloto vira a voz de orientação ao vivo.
- Warn no Copiloto **reusa o texto já localizado** da anomalia (zero i18n novo).
- Regra unificadora: **tier superior suprime o redundante do inferior**.

## Arquivos principais
- `src/anomalies.ts` — `ANOMALY_COVERS_TIP` + `suppressCoveredTips` (puro, testado)
- `src/extension.ts` — `currentAnomalies()`; crit→`alertOut`; `tips` filtrado; `fitsUntilReset` suprimido sob burn rate
- `src/panel.ts` — card Copiloto renderiza anomalias ⚠ warn (de `d.cost.anomalies`)

## Próxima sessão
- [ ] Validar "burn rate suprime 'Cabem ~X'" com oauth ativo (hoje caiu p/ ccusage por 429, não deu pra ver)
- [ ] Refino: anomalia crítica no banner poderia forçar vermelho (err) em vez de herdar a cor da cota
- [ ] Roadmap: #10 multi-conta; ideias novas #12 (previsão estatística) e #13 (custo por branch/PR)

## Contexto importante
- Marketplace bloqueado (publisher `bortolabs`); release publica só no **Open VSX**. VS Marketplace em `continue-on-error`.

## RTK Gain (sessão)
- Economizado nesta sessão: ~0.3k tokens (27 comandos)
- Acumulado 7d (projeto): ~120k
