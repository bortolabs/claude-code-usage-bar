# Handoff — 01/07/2026 (3)

**Chat:** `CCUB-S3_2026-07-01_previsao-estatistica-cota`
**Branch:** `master`
**Último commit:** `3fdef43 — feat: previsão estatística de fim-de-cota #12 (v0.38.0)`
**Release:** `v0.38.0` (CI → Open VSX + GitHub Release)

## Feito
- **Previsão estatística #12 (v0.38.0), end-to-end.** Motor puro `src/core/forecast.ts`: projeção da janela 5h ponderada pela curva histórica (heatmap 7×24), não-linear — `ritmo = uso%/intensidade-decorrida`, `proj = uso% + ritmo × intensidade-restante`. Uniforme colapsa no linear (invariante testado).
- **Tier 1:** sub-linha no alerta de burn rate — "No seu padrão de uso: ~X% no reset — {cabe/no limite/confirma}". **Tier 2:** dica "melhor horário p/ tarefa pesada" (hora mais leve das próximas 12h), gated (uso≥50%, sem alerta, hora≠agora). i18n pt/en/es/fr/de.
- **P0 da S2 validado:** oauth voltou (200, five_hour=76% — o 429 sumiu). A supressão inline do "Cabem ~X" sob burn rate virou helper puro `suppressUnderBurnRate` + testes.
- Higiene: `.vscodeignore`/`.gitignore` excluem dev-dirs (`.vexp/`,`.rtk/`,`.claude/`,`docs/`). Suíte 97 → **113** verdes.

## Decisões
- Forecast só **refina** o alerta (texto); o **gatilho segue linear** de propósito — não silenciar aviso de segurança com base em média histórica. Respeita a hierarquia de tiers da 0.37.0 (uma voz por situação).

## Arquivos principais
- `src/core/forecast.ts` — `forecastFiveHour` + `lightestUpcomingHour` (puros, testados)
- `src/alerts.ts` — `AlertInput.heatmap`; sub-linha #12 quando `plan5h` dispara
- `src/extension.ts` — passa heatmap ao alerta; dica `heavyWorkWindow` no Copiloto
- `src/advisor.ts` — `suppressUnderBurnRate` (guard da 0.37.0)

## Próxima sessão
- [ ] **Validar CI da v0.38.0** (Release no GitHub + Open VSX) e instalar o `.vsix` publicado
- [ ] Ver o #12 ao vivo no painel (sub-linha do alerta + dica de horário) com oauth ativo
- [ ] Roadmap: #13 (custo por branch/PR) · #11 auto-piloto · #10 multi-conta

## Contexto importante
- Marketplace bloqueado (publisher `bortolabs`); release publica só no **Open VSX**. VS Marketplace em `continue-on-error`.
- Empacotar local exige `.vscodeignore` atual (o socket `.vexp/daemon.sock` quebrava o scan de segredos do `vsce`).

## RTK Gain (sessão)
- Economizado nesta sessão: ~281k tokens (44 comandos)
- Acumulado 7d (projeto): ~401k
