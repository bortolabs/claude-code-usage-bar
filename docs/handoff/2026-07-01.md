# Handoff — 01/07/2026

**Chat:** `CCUB-S1_2026-07-01_detector-anomalias`
**Branch:** `master`
**Último commit:** `tag v0.36.0` (release desta sessão)
**Release:** `v0.36.0` (CI → Open VSX + GitHub Release)

## Feito
- **Detector de anomalias (roadmap #4):** `src/anomalies.ts` novo (toolLoop/ctxInflated/cacheLow/mcpRunaway), sinais escalares em streaming no `transcriptStats`, card no painel + seção no dashboard + notificação opt-in. 6 settings + i18n nos 5 idiomas.
- **Economia de tokens:** `CLAUDE.md` novo (RTK + frugalidade), skills `/oi` e `/tchau` adaptadas (modo leve, `CCUB-S{N}`, `--deploy` = release), `.gitignore` (`.rtk/`), `docs/handoff/`.
- Memória `token-economy-priority`.

## Decisões
- `toolLoop` casa **name+input** (não só name) → evita falso positivo com N tool calls paralelas de args diferentes.
- Thresholds ficam em `computeAnomalies` (fora do cache de stats); o corte por-turno (200k) é constante de código de propósito.
- `/tchau --deploy` = fluxo real da extensão (bump + CHANGELOG + tag → CI publica), sem PR/merge.

## Arquivos principais
- `src/anomalies.ts` — motor puro (novo)
- `src/transcriptStats.ts` — sinais `ctxInflatedTurns`/`maxToolRunLength` no `onLine`
- `src/extension.ts` · `src/panel.ts` · `src/dashboard.ts` — wiring + UI + notificação

## Próxima sessão
- [ ] Validar o card de anomalias na extensão rodando (F5) com transcript real
- [ ] Avaliar `toolLoop` nível B (cross-turn na mesma sessão) se o nível A gerar ruído
- [ ] Roadmap: só resta o #10 (multi-conta/perfis)

## Contexto importante
- Marketplace ainda bloqueado (publisher `bortolabs`); o release publica só no **Open VSX**. VS Marketplace roda em `continue-on-error`.

## RTK Gain (hoje)
- Economizado hoje (todas as sessões): ~118k tokens (276 comandos)
- Acumulado 7d (projeto): ~119k
