# Handoff — 02/07/2026

**Chat:** `CCUB-S4_2026-07-02_card-contexto-sessao`
**Branch:** `master`
**Último commit:** `371184e — feat: card "Contexto" na aba Sessão (v0.39.0)`
**Release:** `v0.39.0` (CI → Open VSX + GitHub Release)

## Feito
- **Card "Contexto" na aba Sessão (v0.39.0).** Substitui a linha escalar "Contexto — 14%" por um
  card dedicado (acima de "Fonte de Dados") que reproduz o topo do `/context`: `142.5k / 1M · 14%`
  + barra + rows Usado/Espaço livre. Fiel ao número real (soma `input+cache_read+cache_creation`
  do último turno ÷ janela do modelo).
- **Motor puro `contextFromUsage`** (`src/transcript.ts`) — antes tokens/janela eram calculados e
  descartados; agora propagam por toda a pipeline até o `PanelData.context`. 5 testes novos.
- `contextTokens`/`contextWindow` também no export JSON (p/ agentes). i18n "Usado"/"Espaço livre"
  em pt/en/es/fr/de. Suíte 113 → 118.

## Decisões
- **Só o agregado usado/livre, não as 7 categorias do `/context`.** O breakdown por categoria
  (system prompt/tools/MCP/memory/skills/messages) NÃO é persistido em lugar legível — Claude Code
  calcula em memória e descarta. Estimar divergiria do real; melhor mostrar só o que é 100% fiel.

## Arquivos principais
- `src/transcript.ts` — `contextFromUsage` (puro) + `CurrentTurn.contextTokens/Window`
- `src/extension.ts` — propaga tokens/janela (readState→View→PanelData.context); removeu a row
- `src/panel.ts` — `contextCard` (molde do bucketsCard) montado antes do `sourceCard`

## Próxima sessão
- [ ] **Validar CI da v0.39.0** (GitHub Release + Open VSX) e instalar o `.vsix`
- [ ] Ver o card ao vivo e cruzar o número com o `/context` real do Claude Code
- [ ] Roadmap: #13 (custo por branch/PR) · #11 auto-piloto · #10 multi-conta

## Contexto importante
- `tabBody('sessao')` é reusado pela sidebar E pelo dashboard completo — o card aparece nos dois.
- Marketplace bloqueado (publisher `bortolabs`); release publica só no **Open VSX**.

## RTK Gain (sessão)
- Economizado nesta sessão: ~37.4k tokens (86 comandos)
- Acumulado 7d (projeto): ~426k
