# Handoff — 05/07/2026

**Chat:** `CCUB-S5_2026-07-05_custo-por-branch`
**Branch:** `master`
**Último commit:** `f059e05 — feat: custo por branch/PR #13 — card, dashboard e export (v0.40.0)`
**Release:** `v0.40.0` (CI → Open VSX + GitHub Release)

## Feito
- **Validação da v0.39.0**: CI verde, Open VSX publicado, `.vsix` instalado, card "Contexto"
  confirmado ao vivo (print do usuário batia com o export).
- **#13 Custo por branch/PR (v0.40.0)**: motor `src/branchTimeline.ts` cruza timestamp de
  cada turno com `git reflog --date=unix` (checkouts → intervalos; resolvedor lazy/memoizado
  por repo, `GitRunner` injetável). `TranscriptStats.byBranch` + card "Por branch" na aba
  Custos + breakdown no dashboard + `byProject`/`byBranch` no export JSON + README + i18n
  (5 idiomas). Suíte 118 → 132 (+13: `branchTimeline.test.ts` e 2 no `transcriptStats`).
- Verificado ponta-a-ponta: harness real (30d de transcripts, multi-repo) e harness do
  dashboard (esbuild + stub vscode, HTML estático com 6 asserções).

## Decisões
- **Atribuição por reflog×timestamp** (não "branch atual do cwd") — separa trabalho da mesma
  sessão em várias branches; reflog expira ~90d (janela máx. das quebras é 30d, ok).
- **Sidechains contam no branch REAL** (via cwd), não no projeto sintético "subagentes" — o
  custo do subagente pertence à tarefa. Por isso byBranch ≥ byProject pro mesmo repo.
- **Export mantém `v: 2`** (aditivo, precedente contextTokens/sevenDaySonnet sem bump).
- Chave interna byBranch = projeto+separador(U+001F)+branch — não funde homônimos entre repos.

## Arquivos principais
- `src/branchTimeline.ts` (novo) — parseCheckouts/buildIntervals/branchAt/buildBranchResolver
- `src/transcriptStats.ts` — byBranch na pipeline (Accum.addBranch, onLine, resolver injetável)
- `src/panel.ts` + `src/dashboard.ts` + `src/extension.ts` — card, breakdown, export, builders

## Próxima sessão
- [ ] **Validar CI da v0.40.0** (GitHub Release + Open VSX), instalar `.vsix`, ver card
      "Por branch" ao vivo e conferir `byProject`/`byBranch` no `~/.claude/usage-bar.json`
- [ ] Roadmap: #11 auto-piloto de cota · #10 multi-conta · #14 benchmark comunitário

## Contexto importante
- Label no dashboard/painel prefixa projeto quando há >1 repo (`meu-app · master`).
- Marketplace segue bloqueado (publisher `bortolabs`); release publica só no **Open VSX**.

## RTK Gain (sessão)
- Economizado nesta sessão: ~64.6k tokens (110 comandos)
- Acumulado 7d (projeto): ~450k
