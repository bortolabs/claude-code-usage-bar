# Handoff — 26/07/2026 (S7)

**Chat:** `CCUB-S7_2026-07-26_settings-docs-vexp-mcp`
**Branch:** `master` (**1 commit à frente do origin — falta `git push`**)
**Último commit:** `c9911bb — docs: documenta os 21 settings ausentes e agrupa a tabela por seção`
**Release:** nenhuma (decisão consciente — ver Decisões)

## Feito
- **P0 da S6 validado em parte**: com 2 janelas abertas, o card Contexto bate com o `/context`
  nativo em cada uma (mapa-v2 56.6k/1M·6% · usage-bar 174.2k/1M·17%). O contexto por projeto
  da v0.41.0 está correto ao vivo.
- **Os 21 settings ausentes documentados** nos 2 READMEs: de 42 → **63 chaves**, em 11
  subseções que espelham o `SETTINGS_SCHEMA` da aba Config. Texto reusado dos `package.nls*`.
- **`test/settingsDocs.test.ts`** (novo, 6 casos): trava a defasagem — falha a CI se um setting
  entrar sem doc, se um README citar chave inexistente ou se os dois divergirem. Suíte 151→157.
- **`CLAUDE.md`**: checklist de setting vira **"5 lugares"** (READMEs no passo 5).
- Removida a linha "🎉 1.300+ downloads" do topo dos READMEs (o badge acima já é dinâmico).
- **vexp destravado** (config local, fora do git): `~/.claude.json` e `.vscode/mcp.json`
  apontavam para a extensão **2.1.0**, apagada — instaladas são 2.3.0/2.3.1. MCP dava
  `Failed to connect` enquanto o daemon 2.3.1 seguia vivo, então o guard negava Grep mandando
  usar `run_pipeline` inexistente. Agora `✔ Connected` e as tools respondem.

## Decisões
- **Sem release.** A 0.41.0 e a 0.41.1 saíram hoje; uma 0.41.2 só de docs seria a 3ª em 24h e
  queimaria uma notificação do `updateCheck` (que acabou de entrar) sem nada novo no app. A
  tabela nova sobe junto com a próxima release que tiver código.
- Wrapper `~/.claude/bin/vexp-mcp.sh` resolve a extensão vexp mais recente em runtime
  (`ls .../vexp.vexp-vscode-*/dist/mcp-server.cjs | sort -V | tail -1`) em vez de caminho com
  versão fixa — que quebrava **em todo update**, e de forma silenciosa.
- A mensagem de `deny` do `vexp-guard.sh` passa a ensinar o fallback (`rtk proxy grep -n`),
  para o caso de o MCP cair de novo: custa zero token e chega no instante do bloqueio.
- README agrupado por seção (e não tabela flat de 63 linhas) para casar 1:1 com a aba Config.

## Arquivos principais
- `README.md` · `README.pt-BR.md` — seção de settings reescrita (11 subseções, 63 chaves)
- `test/settingsDocs.test.ts` (novo) · `CLAUDE.md` (checklist "5 lugares")
- Fora do git: `~/.claude/bin/vexp-mcp.sh` (novo), `~/.claude.json` (MCP user), `.vscode/mcp.json`,
  `.claude/hooks/vexp-guard.sh`

## Próxima sessão
- [ ] **`git push`** — o commit `c9911bb` está só local
- [ ] Terminar o P0: validar **timeout de 600s do LLM local** (AI advice) e o **aviso de
      versão nova** — os 2 nunca rodaram no app, só em teste
- [ ] Decidir: anomalia de contexto ("317 turnos acima de 200k") aparece **idêntica nas 2
      janelas**. É coerente com a regra (anomalias são da conta), mas a ação que ela sugere
      (`/compact`, `/clear`) é da janela — escopar por projeto?
- [ ] Roadmap: #11 auto-piloto de cota · #10 multi-conta · #14 benchmark comunitário

## Contexto importante
- **A config de MCP que vale para o Claude Code é `~/.claude.json`** (escopo user), não o
  `.vscode/mcp.json` — editar só o segundo não muda nada. Usar `claude mcp remove/add --scope user`.
- O `.vexp/manifest.json` rastreia hash de `.claude/CLAUDE.md` e de `.claude/hooks/vexp-guard.sh`,
  e o `.claude/CLAUDE.md` é **bloco gerado** (`<!-- vexp v2.1.0 -->`). Se a extensão reescrever
  o guard, a mensagem de fallback se perde — reaplicar. Não editar o `.claude/CLAUDE.md` à mão.
- Backup do `~/.claude.json` pré-mudança ficou no scratchpad da sessão (`claude.json.bak`).

## RTK Gain (sessão)
- Economizado nesta sessão: ~14.5k tokens (109 comandos)
- Acumulado 7d (projeto): ~489k
