# Handoff — 26/07/2026

**Chat:** `CCUB-S6_2026-07-26_dedup-contexto-readme-en`
**Branch:** `master`
**Último commit:** `843e6e6 — docs: README em inglês na página do Open VSX + handoff da sessão S6 (v0.41.1)`
**Release:** `v0.41.0` (dedup + contexto por projeto + aviso de versão) e `v0.41.1` (README EN) — CI verde nas duas

## Feito
- **Validado o P0 da S5** (v0.40.0 ao vivo) — e a validação achou 2 bugs, ambos corrigidos.
- **Dedup de turnos (v0.41.0)**: transcripts gravam SNAPSHOTS DE STREAMING do mesmo turno
  (mesmo `message.id`+`requestId`, `output_tokens` crescendo). Somar todos inflava as quebras
  ~3x. `processFile` passa a ficar com a ÚLTIMA ocorrência. Medido: $139.56 → $59.93 contra
  $60.21 do `ccusage daily` (0,5%).
- **Contexto por projeto (v0.41.0)**: `readCurrentTurn(workspacePaths)` restringe a busca aos
  transcripts da janela. Antes, 2 janelas em projetos distintos mostravam o mesmo número.
- **Aviso de versão nova** (`updateCheck.ts`, Open VSX, 1x/dia, opt-out) — quem instala pelo
  `.vsix` nunca era notificado.
- **AI advice**: timeout 600s p/ endpoint local; presets → `llama3.3`/`gemini-flash-latest`.
- **README.md agora é em inglês** (alcance no Open VSX) + `README.pt-BR.md` via `git mv`.
  `CLAUDE.md` ampliado (mapa do código, convenções, "4 lugares de um setting").
- `.gitignore` dos arquivos do vexp; hook movido p/ `.claude/settings.local.json`.
- Suíte 132 → **151**.

## Decisões
- Dedup fica com a ÚLTIMA cópia (a primeira perderia output); **por arquivo** — 0 duplicatas
  cruzam arquivo em 40k chaves/30d. Confere `cwd` porque o slug do diretório é lossy.
- Projeto sem transcript → card some (não mostra o de outro) e a statusline (arquivo único da
  máquina) deixa de ser fallback do contexto quando há escopo.
- Config do vexp NÃO commitada: instrui tools que o contribuidor não tem e tem caminho absoluto.

## Arquivos principais
- `src/transcriptStats.ts` — `turnKey` + 2 fases em `processFile`
- `src/transcript.ts` — `projectSlug`, `readCurrentTurn(workspacePaths)`, campo `scoped`
- `src/updateCheck.ts` (novo) · `src/extension.ts` (escopo + reset sticky + gate do fallback)

## Próxima sessão
- [ ] **Instalar o `.vsix`** e validar ao vivo: contexto correto nas 2 janelas, timeout do LLM
      local e o aviso de update (os 3 nunca rodaram no app, só em teste)
- [ ] Documentar os **21 settings ausentes** nos 2 READMEs (`advisor*`, `anomaly*`, `aiAdvice*`,
      `history*`, `tokenGoal*`, `tooltipDetail`, `dashboardWindow`, `weeklySummaryEnabled`)
- [ ] Roadmap: #11 auto-piloto de cota · #10 multi-conta · #14 benchmark comunitário

## Contexto importante
- O README **é empacotado no `.vsix`** — a página do Open VSX só mostra o inglês a partir da
  0.41.1. Mudou um README, muda o outro; links entre eles têm que ser URL absoluta.
- Export JSON é arquivo único: com 2 janelas, cada uma grava o contexto do seu projeto,
  alternadamente (usar `exportStatePath` distinto por workspace).
- O `.claude/CLAUDE.md` do vexp (local, ignorado) manda "NÃO use grep/glob" e contradiz o
  `CLAUDE.md` da raiz — foi o que bloqueou `Grep` nesta sessão (contorno: `rtk proxy grep`).

## RTK Gain (sessão)
- Economizado nesta sessão: ~31.3k tokens (259 comandos)
- Acumulado 7d (projeto): ~470k
